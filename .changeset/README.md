# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

Add a changeset for any change that should bump the plugin version:

```sh
pnpm changeset
```

Pick a bump type (`patch` / `minor` / `major`) and describe the change. Commit
the generated markdown file with your PR. See `RELEASE.md` for the full flow.
