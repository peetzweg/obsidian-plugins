{
  "name": "obsidian-plugins",
  "private": true,
  "scripts": {
	"postinstall": "./scripts/install-plugins-to-vault.sh",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "descriptors": "pnpm --filter @repo/descriptors papi:update"
  },
  "devDependencies": {
    "@total-typescript/tsconfig": "^1.0.4",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "turbo": "^2.5.4",
    "typescript": "5.8.3",
    "vitest": "^3.2.4"

  },
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=20"
  }
}
